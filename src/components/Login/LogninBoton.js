import React from "react";
import { Link } from "react-router-dom";

const LognInBoton = () => {
 return(
    <Link to='/login' className='nav-links-mobile'>
        Sign Up
    </Link>
 )
};

export default LognInBoton;